# STEP — Python dependencies
# Trained/tested on Python 3.10, CUDA 12.8, NVIDIA RTX 5070 Ti.
#
# NOTE on PyTorch: install the build matching your CUDA version, e.g.
#   pip install torch torchvision --index-url https://download.pytorch.org/whl/cu124
# NOTE on ANARCI: most reliably installed via bioconda:
#   conda install -c bioconda anarci

torch>=2.1.0
torchvision>=0.16.0
numpy>=1.23.0
pandas>=1.5.0
scikit-learn>=1.1.0
scipy>=1.9.0
matplotlib>=3.6.0
seaborn>=0.12.0
umap-learn>=0.5.3
biopython>=1.80
anarci>=1.3
shap>=0.41.0
tqdm>=4.64.0
